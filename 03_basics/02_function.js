function calculatecartprice(...num1) // with ... the values goes into an array
{
   return num1
}
console.log(calculatecartprice(100 , 200 , 300 , 20000))
const user = {
    username: "Dev",
    price: 109
}
function handleObject(anyobject)
{
    console.log(`Username is ${anyobject.username} and price is ${anyobject.price}`)
}

handleObject(user)
