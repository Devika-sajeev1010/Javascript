 if(true)
 {
    let a =10
    const b =60
     var c = 70
 }
 console.log(a) // it will show that a ,b are not defined.
 console.log(b)
 console.log(c) // but it will print 70 due to var keyword
//  Function Scope (var): Lives as long as the function runs.
// Block Scope (let, const): Lives only inside { ... } like if, for, or {}.
 